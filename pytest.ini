[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --strict-markers
    --tb=short
    -ra
markers =
    unit: Unit tests
    integration: Integration tests with cloud providers
    slow: Slow tests
